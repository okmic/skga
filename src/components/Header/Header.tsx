import { memo } from "react"
import { HeaderDescription, Navigate, WrapperHeader } from "./index.styled"

const Header = () => {
    return <WrapperHeader>
        <HeaderDescription>

        </HeaderDescription>
        <Navigate>
            
        </Navigate>
    </WrapperHeader>
}


export default memo(Header)